/**
 * An object that interacts with a Canvas object through a listener/callback model.
 */
public interface CanvasInterface {
    /**
     * Stops the listener in question.
     */
    public void stopCanvasListener();

    /**
     * Starts the listener in question.
     */
    public void startCanvasListener();
}
